<%#
# Header actions of edit page

This partial is used to display the header actions of the edit page.

## Local variables:

- `page`:
  An instance of [Administrate::Page::Form][1].
  Contains helper methods to help display a form,
  and knows which attributes should be displayed in the resource's form.

[1]: http://www.rubydoc.info/gems/administrate/Administrate/Page/Form
%>

<%= link_to(
      t('administrate.actions.show_resource',
        resource_name: display_resource_name(resource_name, count=1),
        default: "Show #{page.page_title}"),
      [namespace, page.resource],
      class: "button",
    ) %>