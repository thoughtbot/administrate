dependencies:
  post:
    - bundle exec appraisal install
database:
  override:
    - bundle exec appraisal rails-4 rake db:create
test:
  pre:
    - cp .sample.env .env
    - bundle exec appraisal rails-4 rake db:schema:load
  override:
    - bundle exec appraisal rake spec
