<header class="header">
  <h1 class="header-heading"><%= @page.page_title %></h1>
  <%= link_to 'Edit', [:edit, @page.resource], class: "button" %>
</header>

<dl>
  <% @page.attributes.each do |attribute| %>
    <dt><%= attribute.name.titleize %></dt>
    <dd><%= render attribute %></dd>
  <% end %>
</dl>
